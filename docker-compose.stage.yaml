version: "3"

services:
  olegmaslovsky/angular-hw:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        buildscript: build-stage
        mode: stage
    restart: always
    ports:
      - 80:80
      - 443:443
